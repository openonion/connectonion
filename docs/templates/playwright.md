# Playwright Template (Renamed)

The `playwright` template has been renamed to `browser`.

Use `--template browser` instead:

```bash
co create browser-bot --template browser
```

See [browser.md](browser.md) for full documentation.
