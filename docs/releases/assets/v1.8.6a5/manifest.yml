version: v1.8.6a5
# Both blocks are the real commands, run against the inbox the WhatsApp
# acceptance left behind, captured from their own stdout. Generated by
# scripts/capture_186a5_release_cli.py.
images:
- file: find-a-conversation-and-read-it-back.png
  alt: "co whatsapp chats listing a direct chat and a group with message counts and
    last activity, then co whatsapp log --chat reading four messages back from that
    group including ones that named nobody."
  caption: "The chat id that send and reply both need now comes from a command, and
    a conversation can be read back including the messages that never named the bot."
  scenario: "Run against the 11-record inbox from the 2026-09-17 WhatsApp acceptance,
    five of whose messages are mentioned:false. The group shows 7 messages and 2
    for-us — that gap is the conversation happening around the bot. The records have
    no sender_name or kind because they were written by 1.8.6a2 and are read back as
    they were stored, which is what backwards compatibility looks like from the
    outside."
  viewport: 1040x~390 terminal capture, 2x
  commit: 5cbdc8b0
  source_run: docs/acceptance/1.8.6/whatsapp-live-2026-09-17.md
