version: v1.8.6a6
# The connection half is the live listener's own log from the owner's real
# account — a genuine reconnection that happened while this release was being
# prepared. The record half is the release's own Message shape, serialised by
# running it. Generated by scripts/capture_186a6_release_cli.py.
images:
- file: events-that-used-to-be-silence.png
  alt: An inbox log showing a disconnection and the reconnection a second later, then
    two records — one for a message that could not be decrypted and one for the bot
    being added to a group.
  caption: Things that happen to the account stop being silence — a disconnection
    leaves a trace, and a message that could not be read is a record rather than
    nothing.
  scenario: "The log lines are live, from the real linked number at 2026-09-18
    21:54: a socket dropped and came back in one second. On the code this release
    replaces, that reconnection would have appeared only as an unexplained second
    'connected as' line — which is exactly what the previous 42 hours of that log
    contains eight of. The two records are the shapes an UndecryptableMessageEv and
    a JoinedGroupEv now produce; before this release both were discarded."
  viewport: 1040x~290 terminal capture, 2x
  commit: 2c962dd2
  source_run: https://github.com/openonion/connectonion/issues/1590
