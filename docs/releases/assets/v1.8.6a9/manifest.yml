version: v1.8.6a9
# The "before" block is not a re-enactment: the id and the JSON row are the
# real record the bug left in the account's own sent.jsonl on 19 September,
# when a blank reached a customer's group. The "after" block is the shipped
# command run against a scratch inbox, stderr before stdout so the refusal
# appears where a terminal would print it.
# Generated by scripts/capture_186a9_release_cli.py.
images:
- file: nothing-is-not-a-message.png
  alt: "co whatsapp send with no text on 1.8.6a8, printing a message id, exit
    code 0 and a sent.jsonl row whose text field is empty and whose ok field is
    true; then the same command on 1.8.6a9, printing nothing to send and exit
    code 2."
  caption: "An empty message used to be a successful send of nothing — id, exit
    0, a row in the log saying it worked. It is a usage error now."
  scenario: "The top block is what shipped in a8 and what actually happened
    twice on 19 September: the command read stdin, reached EOF, and delivered a
    blank bubble into a real group. Everything that normally catches a bad send
    reported success, including reading the record back, because the record was
    of a real send. The bottom block is a9: nothing is sent, nothing is logged,
    and the exit code is the same usage error as running it on a terminal with
    no text."
  viewport: 1040x~580 terminal capture, 2x
  commit: 566b2f6f
  source_run: https://github.com/openonion/connectonion/issues/1602
