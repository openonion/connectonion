version: v1.8.6a8
# Both blocks are real stdout from the shipped code paths. The provider is the
# real WhatsApp one with only its socket replaced, so render(), the handler and
# the sent.jsonl write are all the code that ships; nothing is sent.
# Generated by scripts/capture_186a8_release_cli.py.
images:
- file: markdown-becomes-what-whatsapp-draws.png
  alt: "co whatsapp send with a Markdown message containing double-asterisk bold
    and a bracketed link, printing a message id; then the same message read back
    out of sent.jsonl, showing single-asterisk bold and the link flattened to
    label and URL."
  caption: "What the agent writes is Markdown; what the group receives is
    WhatsApp's own marks — and the record now holds the second one, so the log
    shows the message that was actually sent."
  scenario: "The first block sends a line with `**finished**` and
    `[the run](https://ci/42)` in it. The second reads the last row of
    sent.jsonl, which before this release held the Markdown as typed even
    though the platform received the translation — so `log` showed a message
    nobody in the chat had seen. Rendering now happens once, in the caller, and
    the rendered string is both what goes over the wire and what is recorded."
  viewport: 1040x~315 terminal capture, 2x
  commit: 950b2fe7
  source_run: https://github.com/openonion/connectonion/pull/1600
