# 1.8.4 / 1.8.4a1 release readiness

Updated 8 September 2026. The owner explicitly approved technical-article model
review and the prepared Gmail/Drive fixture journey, then requested **1.8.4a1
preview publication first, followed by local installed-package testing**. Stable
remains 1.8.3; final 1.8.4 still needs real Gmail/Drive and physical NAS acceptance.
Hosting remains local; no new cloud resources are authorized or needed.

The prior local candidate record below preserves the tested source/artifact
identities. Its pending-export descriptions are superseded by the explicit
approval above. Five feature journals passed the local model check (the Control
Center article was revised around its actual regression); all five feature PRs passed their current checks and are merged into main
(`#1452`, `#1455`, `#1435`, `#1365`, `#993`; main `ae40be9d`). The preview's own Design Journal also passed locally.

Preview metadata is `1.8.4a1` with the Alpha package classifier. The protected tag
workflow must publish it as a GitHub prerelease, with `latest=false`, and verify
the public artifacts. Post-publication tests must import the downloaded preview,
not the source checkout. The prepared live Gmail test is authorized, but has not
run yet; it will run after publication as requested. No NAS profile is configured.

# Earlier integrated-candidate evidence

Updated 7 September 2026. The complete implementation is combined on
`review/1.8.4-integration`; the original dirty project checkouts were preserved.
**Core 1.8.4 is prepared locally, not published.** The paired React SDK
`0.4.4-rc.2` is published and consumed through O Chat's normal lockfile.
The operator requested local hosting acceptance; no cloud bucket, IAM change,
production hosting process or domain was provisioned.

- **Proposed target version:** 1.8.4
- **Estimated release window:** after the remaining acceptance and PR gates
- **Forward-port tracking issue (stable patches only):** N/A — mainline work

## Implemented scope and PR state

| Work | Current state |
| --- | --- |
| Mainline metadata policy | [#1449](https://github.com/openonion/connectonion/pull/1449) merged; current mainline checks pass |
| Global env / provider records #1444 | [#1450](https://github.com/openonion/connectonion/pull/1450) merged after runtime/platform and read-only provider acceptance |
| Gmail frozen listings #1446 | [#1451](https://github.com/openonion/connectonion/pull/1451) merged with passing current checks |
| Gmail mailbox / incoming attachments #1446 | [#1452](https://github.com/openonion/connectonion/pull/1452); runtime head `8dbe7d0d`; revised journal remains local at `213f27dd` |
| Gmail exact-content draft review #1424 | Draft [#1455](https://github.com/openonion/connectonion/pull/1455), code-only head `7b5101fa`; live fixture execution and journal review remain pending |
| Synology 20-command core #1429 | [#1435](https://github.com/openonion/connectonion/pull/1435), code-only head `7180d233`; full 20-command implementation is reviewable, journal remains local |
| Control Center Core #1354 | [#1365](https://github.com/openonion/connectonion/pull/1365), code-only head `b9c1c511`; full runtime is reviewable, journal remains local |
| React SDK | [#103](https://github.com/openonion/connectonion-react/pull/103) merged; `0.4.4-rc.2` published from merge `8a0497bd` |
| O Chat | [#241](https://github.com/openonion/oo-chat/pull/241), head `5ed55728`; exact published SDK pin and local 220-test browser acceptance |
| Immutable app hosting | [#235](https://github.com/openonion/oo-api/pull/235), head `f02a9be` (documentation follow-up to tested `3ea2c202`); GCS generation-race fix and passing local emulator acceptance |
| Candidate docs | [#130](https://github.com/wu-changxing/connectonion-docs-site/pull/130), head `6da8670`; SDK publication documented, Core stable remains 1.8.3 |
| Agent identity diagnostics | [#993](https://github.com/openonion/connectonion/pull/993); runtime/docs repaired remotely; new local journal at `a1e86ec2` awaits review/export permission |
| Optional subagent registry cleanup | [#1207](https://github.com/openonion/connectonion/pull/1207) merged; contributor history preserved and project override regression passes |

## Validation completed

- **Combined Core:** 8,562 passed, 23 skipped, 184 live tests deselected, six
  warnings, 305.58 seconds at integration `3f389811`. This includes all feature
  slices, merged main and the published browser SDK provenance. The subsequent
  1.8.4 metadata preparation passed 34 version/release checks, with one docs-site
  checkout test skipped; the separate docs repository was verified directly.
- **Current code-only PR heads:** Synology 156, Control Center 162, and Gmail/Drive
  357 focused tests pass after integration with their public bases. These heads
  contain no changed blog files or new private journal objects, so runtime CI
  can run without exporting the pending articles.
- **React:** 169 tests in 19 suites, type checking and module/browser builds.
  Protected publication also passed audit and clean tarball installation.
- **O Chat:** clean registry-based `npm ci`, 213 unit tests in 27 files, type
  checking, full lint (zero errors, seven existing warnings), production Webpack
  build, and **220 Playwright tests in 4.6 minutes**. Desktop, mobile, blocked
  review and continuous-journey screenshots were inspected and committed.
- **Browser capabilities:** real Chromium tests exercise ordinary network APIs,
  storage/IndexedDB, Workers, Canvas/SVG, WebGL and WASM. Two added cases verify
  denied undeclared media, declared embedded/top-level media with synthetic
  devices, fullscreen and clipboard delegation without clipboard access.
  These do not capture real microphone/camera streams.
- **Hosting API:** 855 passed, 168 skipped. Three new regressions reproduce the
  GCS metadata/download generation race, bounded contention and size rechecks.
  The opt-in local emulator run passes eight concurrent uploads, exact quota
  accounting, idempotency, static byte/header checks and corruption rejection;
  fixture cleanup is confirmed. It uses anonymous credentials and loopback only.
- **Docs:** production build, type checking, SEO/blog checks and 1440px/375px
  visual/link checks passed earlier; the latest SDK documentation update also
  has green hosted verification, SEO and Vercel checks. Stable advertising
  remains 1.8.3 until Core is actually published.
- **Credential/provider acceptance:** six-provider installed read-only checks
  passed using the designated env and unrelated cwd. Gmail draft list, Drive
  listing and restarted frozen-row info passed without recording private content.
- **CLI contracts:** generated help/packaged-skill parity, piped next-command
  discovery and error/exit cases are recorded in each component acceptance file.
  CI-colored Gmail and Synology help assertions were reproduced and fixed.
- **Live synthetic reviewer:** a tiny synthetic HTML/JS app was approved by
  `co/gemini-3.8-flash`, with no findings and reported cost $0.00584475. This
  proves the installed reviewer invocation, not a review of a real authored app.

The original O Chat registry build failure is resolved by the exact SDK pin;
the current head `5ed55728` passed hosted E2E, CodeQL and Vercel. A subsequent
body-only update passed the visual declaration for that same full head; its
skipped E2E job does not replace the already completed passing E2E run.
Current hosted check state is recorded in `1.8.4-ci-snapshot.json`.
All recorded runtime/platform, metadata and companion checks pass at the current
heads. No jobs remain pending. The only failing checks are the required story
gates on mailbox #1452, draft #1455, Synology #1435, Control Center #1365 and
identity #993; their articles remain pending review/export approval.

- **Permanent release visuals:** five inspected assets (desktop, mobile, blocked
  review and installed CLI before/after) are retained under
  `docs/releases/assets/v1.8.4`; `check_release_visuals.py v1.8.4` passes.
  The CLI baseline comes from the public 1.8.3 wheel and uses account-free help
  in a temporary configuration. Candidate release notes are in
  `docs/releases/1.8.4.md`.

## Exact artifacts

The protected SDK workflow published `@connectonion/react@0.4.4-rc.2` under the
`rc` dist-tag. The registry tarball and preserved CI artifact are byte-identical:

`f608a913528beeda6c3ac32072b281ad38cb4d5eadd615824fb1270e2b0d07b2`

The Core browser SDK is byte-identical to that package, SHA-256:

`42282a9abc66f507a4f278b9824a5a91c270c497cbda6a204fbfec7aa8dad837`

The initial local Core 1.8.4 wheel and sdist build and pass Twine checks. The wheel is
installed into a fresh target outside the checkout. Its unrelated-cwd smoke
checks verify version 1.8.4, six starter files, SDK rc.2 provenance, packaged
review policy, approved/blocked/retained/rollback behavior and all relevant CLI
help groups. Tested local wheel SHA-256:

`b4a13861e7dd04b4d316fdd72c17bb984fa90beb7901b219fd3fcdb8292feef6`

This initial artifact predates the final acceptance-document and visual-bundle
updates. It is not a published release. Publication must use the exact
artifact produced by the protected tag workflow after final acceptance.

## Remaining gates

1. **Five journal articles:** the required CI story check sends their complete
   text to the configured Gemini model. Specific export authorization remains
   pending. The full draft/Synology/Control Center code is published for review
   without those articles; the revised mailbox/identity journals also remain
   local. None of the new code-only heads introduces private journal objects
   in its history, and none changes docs/blog relative to its PR base. The
   required story gate remains failing before model export, without a waiver.
2. **Gmail/Drive writes:** the prepared
   [live acceptance script](../../scripts/acceptance/gmail_release.py) requires
   specific permission for one self-addressed send and synthetic fixture writes.
   It verifies local/Drive/native/link round trips, stale-review rejection,
   delivered hashes, consumed draft, receipt recovery, mailbox actions and
   collision-safe downloads. No real Gmail message has been sent by this work.
3. **NAS target:** no saved Synology profile or NAS settings were found in the
   indicated global configuration. Authenticated physical DSM acceptance still
   needs a target/profile. Local regression coverage does not claim a real NAS run.
4. **Final review/publication:** complete outstanding feature PR checks/merges,
   perform the agreed remaining acceptance, publish Core by its reviewed immutable
   tag workflow, verify PyPI/GitHub artifact identity, then update stable docs.
   SDK stable promotion is coordinated with that release.

Hosting acceptance is **local**, as requested. Production DNS/TLS, real GCS and
separate service identities were not exercised and are not claimed. The operator
configuration for an eventual deployment is documented in the hosting PR; this
run does not request or provision a new cloud resource.

## Already shipped and exclusions

Core 1.8.3 is already published (GitHub release 6 September 2026); the old
prepared-only history wording was corrected. Google #1440/#1442 and Gemini #1432
are existing mainline work. Core Gemini `203c846d` and backend #225 merge
`483d0fc6` were verified in their respective main histories. Do not merge the
old Google/TikTok #1439 branch wholesale.

Mail scheduling #1197/#814, Personal Wiki #1443, Sync Knowledge, TikTok/new
messaging and inbound automatic replies remain outside this release. Synology
is not Sync Knowledge. Browser #134 is coordination, not another implementation
project; free system Chrome/Patchright remains the default and paid use explicit.

The combined Core suite covers exact current browser regressions: HTTP 407 and
Chrome network-error documents fail truthfully; whole-browser close waits for
its daemon PID; dead/killed contexts do not report open; a previously absent
browser is detected after installation. No new signing/notarization gate is added.

## Prepared journal drafts

- [The Gap After Review](../blog/2026-09-07-the-gap-after-review.md)
- [The Request Ended. The Copy Did Not.](../blog/2026-09-07-the-request-ended-the-copy-did-not.md)
- [The Page That Survived Its Review](../blog/2026-09-07-the-page-that-survived-its-review.md)
- [A Download Can Partly Succeed](../blog/2026-09-07-a-download-can-partly-succeed.md)
- [The Diagnostic Chose the Wrong Account](../blog/2026-09-07-the-diagnostic-chose-the-wrong-account.md)
