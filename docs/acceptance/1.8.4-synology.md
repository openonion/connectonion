# 1.8.4 Synology acceptance

Candidate implementation for #1429, adapted from #1435 on the shared credential
foundation. No version bump, release, NAS mutation or live NAS acceptance is
claimed by this record.

- 117 focused tests passed for profiles, verified transport, source-separated
  inspection, complete paths/paging/search cleanup, protected transfers,
  copy/move receipts, sharing and the actual twenty-command parser.
- 146 targeted integration regressions passed, including shared typo handling and
  global environment loading. The full repository run at `d913be24` passed:
  **8312 passed, 23 skipped, 184 deselected**, with seven warnings (310.59 seconds).
- Parser and packaged skill agree on all twenty canonical commands and three
  visible aliases. All **21/21** synthetic output-only next-command cases passed
  the pinned `co/gemini-3.7-flash` audit; model replies were never executed.
- Wheel/sdist build, Twine validation, and offline lock validation passed.
  Fresh installed-package processes outside the checkout passed all 23 leaf
  help checks and schema-1 JSON exit-code checks (success, operation and usage).
  Wheel SHA-256:
  `6c7d9e9ad9e11dae5e08b65894e060e3f0c238e260074bb6e2aafae4aad1b0ed`.
- The full implementation is now reviewable in #1435. The Design Journal stays
  local pending approval for its model-backed review/export. The code-only PR
  has no docs/blog diff and the required story gate remains pending, without a waiver.
- The current review code at `4b267d88` passes 156 focused Synology, Gmail listing
  and subagent regressions after merging main. The combined Core candidate
  passed 8,562 tests, with 23 skipped and 184 live tests deselected.
- Real NAS read-only and disposable transfer/share/revoke journeys are pending.
  The authorized `keys.env` contains no NAS settings and no profile was found;
  the user was asked for the existing configuration path/profile name.

The implementation has explicit limits: capability-negotiated DSM APIs, no
proven real model/DSM matrix, missing indicators remain unavailable, SNMP IPv4
only, service enumeration requires the documented SSH command, provider-unit
capacity counters, remote preflight races, and a weaker Windows ancestor-race
boundary than POSIX. Exact renamed cross-folder operations may stage data;
status never submits the next write. See [the decision](../design-decisions/070-synology-inspections-and-durable-operations.md).
